# L11-12 讲义：MCP协议与工具集成

> **课程**: MBA大模型智能体课程  
> **课时**: 第11-12讲（90分钟）  
> **适用**: 讲师备课 / 学生预习与复习  
> **对应Slides**: `slides-marp/day2-lesson11-12-mcp.md`

---

## 本讲核心问题

1. **为什么需要MCP？** 工具集成的痛点是什么？
2. **MCP如何工作？** Host/Client/Server三层架构
3. **Skill是什么？** 与MCP的区别是什么？
4. **如何设计好的工具？** 有哪些原则和反模式？

---

## Part 1：工具集成的痛点

### M×N连接器爆炸

想象你是一家企业的IT负责人：

```
你有 M 个AI应用：
- ChatGPT
- Claude
- 内部Agent
- ...

你有 N 个工具/数据源：
- Slack
- GitHub
- 数据库
- 内部系统
- ...

传统方案：每对都要单独集成
连接器数量 = M × N
```

当M=10，N=20时，你需要维护200个连接器。这是不可持续的。

### 四大痛点

| 痛点 | 表现 | 后果 |
|------|------|------|
| **重复造轮子** | 每个AI应用都重新集成 | 开发成本高 |
| **稳定性差** | 各团队实现质量不一 | 维护成本高 |
| **安全难治理** | 权限分散、审计困难 | 合规风险 |
| **协作成本高** | 接口不统一、文档缺失 | 沟通成本 |

### MCP的解决思路

```
传统: M × N 连接
─────────────────
AI应用1 ──┬── 工具A
          ├── 工具B
          └── 工具C
AI应用2 ──┬── 工具A
          ├── 工具B
          └── 工具C

MCP: M + N 连接
─────────────────
AI应用1 ──┐
AI应用2 ──┼── MCP协议 ──┬── 工具A
AI应用3 ──┘              ├── 工具B
                         └── 工具C
```

**类比**：MCP就像USB协议——在USB出现之前，每种设备都有专属接口；有了USB之后，任何设备都能即插即用。

---

## Part 2：MCP协议原理

### 什么是MCP？

MCP (Model Context Protocol) 是Anthropic于2024年11月发布的开放标准，用于连接AI应用与外部工具/数据。

> **一句话定义**：MCP是AI应用与工具之间的"USB协议"——标准化接口，即插即用。

### 三层架构

```
┌─────────────────────────────────────────────────────┐
│                    MCP 架构                          │
├─────────────────────────────────────────────────────┤
│                                                     │
│  Host（宿主）                                       │
│  └── Claude Desktop、IDE、你的应用                 │
│      职责：管理生命周期、安全策略                  │
│                                                     │
│  Client（客户端）                                   │
│  └── MCP Client库                                  │
│      职责：与Server通信、协议解析                  │
│                                                     │
│  Server（服务端）                                   │
│  └── GitHub MCP、Slack MCP、自定义MCP             │
│      职责：暴露工具能力、处理请求                  │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 四大能力

| 能力 | 说明 | 示例 |
|------|------|------|
| **Tools** | 可执行的函数 | 发送消息、创建文件 |
| **Resources** | 可读取的数据 | 文件内容、数据库记录 |
| **Prompts** | 预定义的提示词模板 | 代码审查模板 |
| **Sampling** | Server请求LLM生成 | 高级场景 |

### 工作流程

```
用户: "帮我创建一个GitHub issue"
        │
        ▼
    ┌─────────┐
    │  Host   │  ← Claude Desktop
    └────┬────┘
         │ 1. 识别需要调用GitHub工具
         ▼
    ┌─────────┐
    │ Client  │
    └────┬────┘
         │ 2. 发送JSON-RPC请求
         ▼
    ┌─────────┐
    │ Server  │  ← GitHub MCP Server
    └────┬────┘
         │ 3. 调用GitHub API
         │ 4. 返回结果
         ▼
    用户看到: "已创建Issue #123"
```

### 传输方式

| 方式 | 适用场景 | 特点 |
|------|----------|------|
| **stdio** | 本地进程 | 简单、安全、无需网络 |
| **HTTP** | 远程服务 | 可扩展、需要认证 |

---

## Part 3：Skill系统

### 什么是Skill？

Skill是OpenClaw的能力扩展机制，核心是一个**SKILL.md**文件——让Agent学习如何使用工具的说明文档。

### 与传统Plugin的区别

```
传统Plugin:
代码 ──► 代码接口 ──► 执行

Skill:
SKILL.md ──► LLM阅读理解 ──► LLM决定如何调用 ──► 执行

关键区别: LLM自己读文档学习使用方法！
```

### 为什么这样设计？

1. **灵活性**：不需要预定义所有调用方式
2. **容错性**：LLM可以处理模糊指令
3. **可扩展**：增加能力只需写文档
4. **可维护**：非程序员也能创建Skill

### Skill结构示例

```
~/.openclaw/workspace/skills/weather/
├── SKILL.md          # 核心说明文档
├── scripts/
│   └── get_weather.sh
└── examples/
    └── usage.md
```

**SKILL.md内容**：

```markdown
# Weather Skill

## 功能
获取指定城市的天气预报。

## 使用方法
运行 `./scripts/get_weather.sh <城市名>`

## 示例
用户: "北京今天天气怎么样？"
动作: 运行 ./scripts/get_weather.sh 北京
输出: 解析结果并回复用户
```

### MCP vs Skill

| 维度 | MCP Server | Skill |
|------|------------|-------|
| 标准化 | JSON-RPC协议 | 无标准 |
| 跨平台 | 任何支持MCP的Host | 仅OpenClaw |
| 实现复杂度 | 中等 | 低 |
| 适用场景 | 通用服务集成 | 特定场景快速实现 |

**选择建议**：
- 需要跨平台 → MCP
- 仅OpenClaw使用 → Skill更简单
- 复杂企业集成 → MCP（标准化）
- 快速原型 → Skill（1小时上线）

---

## Part 4：工具设计原则

### Anthropic的洞察

在[Writing Tools for AI Agents](https://www.anthropic.com/engineering/writing-tools-for-agents)中，Anthropic分享了工具设计的核心原则：

> "One of the most common failure modes we see is **bloated tool sets** that cover too much functionality or lead to ambiguous decision points."

### 原则1：最小化工具集

```
❌ 坏设计：20个功能相近的工具
─────────────────────────────
search_products()
find_products()
query_products()
get_product_list()
lookup_products()
...

问题：Agent困惑"我该用哪个？"


✅ 好设计：最小可行工具集
─────────────────────────────
search_products(query, filters)

清晰、无歧义、一个工具解决一类问题
```

**判断标准**：如果人类工程师都不能确定该用哪个工具，AI也做不到。

### 原则2：清晰的接口描述

```json
{
  "name": "create_issue",
  "description": "在GitHub仓库中创建新的Issue",
  "parameters": {
    "repo": {
      "type": "string",
      "description": "仓库名，格式为 owner/repo"
    },
    "title": {
      "type": "string",
      "description": "Issue标题，简明扼要"
    },
    "body": {
      "type": "string",
      "description": "Issue详细描述，支持Markdown"
    }
  }
}
```

关键：**描述要让LLM能准确判断何时使用、如何使用**。

### 原则3：优雅的错误处理

```python
# 好的错误返回
{
  "success": false,
  "error": {
    "code": "REPO_NOT_FOUND",
    "message": "仓库 'owner/repo' 不存在或无权限访问",
    "suggestion": "请检查仓库名是否正确，或确认有访问权限"
  }
}
```

让Agent能理解错误并采取补救措施。

### 原则4：幂等性

```
同一请求执行多次，结果应该一致

✅ 幂等: get_user(id=123) 总是返回同一用户
✅ 幂等: set_status(id=123, status="done") 重复执行无副作用
❌ 非幂等: create_order() 每次都创建新订单
```

非幂等操作需要额外的确认机制。

### 反模式清单

| 反模式 | 问题 | 改进 |
|--------|------|------|
| 功能重叠 | Agent不知选哪个 | 合并或明确区分 |
| 描述模糊 | 误用率高 | 写清楚使用场景 |
| 错误不明 | Agent无法恢复 | 提供可操作的错误信息 |
| 参数过多 | 调用复杂 | 提供合理默认值 |
| 无权限控制 | 安全风险 | 实现最小权限原则 |

---

## Part 5：企业工具治理

### 安全分层

```
┌─────────────────────────────────────────────────────┐
│                   安全分层模型                       │
├─────────────────────────────────────────────────────┤
│                                                     │
│  L0 只读工具                                        │
│  └── 搜索、查询、读取                              │
│      风险：低，可自由使用                          │
│                                                     │
│  L1 低影响写入                                      │
│  └── 创建草稿、添加备注                            │
│      风险：中低，需要日志                          │
│                                                     │
│  L2 业务影响                                        │
│  └── 发送消息、修改数据                            │
│      风险：中，需要审批或确认                      │
│                                                     │
│  L3 高风险操作                                      │
│  └── 删除、资金操作、权限变更                      │
│      风险：高，必须人工确认                        │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 审计要求

每次工具调用应记录：

```json
{
  "timestamp": "2026-03-01T00:15:00Z",
  "user": "user_123",
  "agent": "assistant",
  "tool": "create_issue",
  "parameters": {"repo": "...", "title": "..."},
  "result": "success",
  "token_cost": 150
}
```

### 成本控制

| 维度 | 控制手段 |
|------|----------|
| 调用频率 | Rate limiting |
| 单次成本 | Token限制 |
| 累计成本 | 预算告警 |
| 异常检测 | 模式识别 |

---

## Part 6：实战演示

### 体验MCP

**Claude Desktop配置示例**：

```json
{
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_TOKEN": "your_token"
      }
    }
  }
}
```

配置后，可以直接对Claude说"帮我创建一个GitHub issue"。

### 创建自定义Skill

**步骤**：

1. 创建目录：`~/.openclaw/workspace/skills/my-skill/`
2. 编写SKILL.md
3. 添加脚本或工具
4. 测试使用

**示例SKILL.md**：

```markdown
# 股票查询 Skill

## 功能
查询A股股票的实时行情。

## 使用方法
运行 `curl "https://api.example.com/stock?code=$CODE"`

## 示例
用户: "茅台现在多少钱？"
动作: 查询股票代码600519的实时价格
```

---

## 课堂实操

### 设计一个MCP Server（15分钟）

选择以下场景之一，设计MCP Server的接口：

1. **企业知识库**：搜索内部文档
2. **CRM系统**：查询客户信息
3. **日程管理**：创建和查询日程

需要定义：
- 提供哪些Tools？
- 每个Tool的参数和返回值？
- 如何处理错误？
- 安全等级是什么？

---

## 本讲总结

### 核心概念

- **MCP = AI应用与工具之间的USB协议**
- **三层架构**：Host / Client / Server
- **四大能力**：Tools / Resources / Prompts / Sampling
- **Skill**：OpenClaw的轻量级能力扩展机制

### 工具设计原则

1. **最小化工具集**：避免功能重叠
2. **清晰的描述**：让LLM能准确判断
3. **优雅的错误处理**：让Agent能恢复
4. **幂等性**：重复执行无副作用

### MBA关键洞察

1. **MCP解决的是协作问题**，不只是调用问题
2. **好的工具设计减少Token消耗**，直接降低成本
3. **安全分层是必须的**，不是可选的
4. **Skill让非技术人员也能扩展AI能力**

---

## 延伸阅读

### 官方资源
- [MCP官网](https://modelcontextprotocol.io/)
- [MCP GitHub](https://github.com/modelcontextprotocol)
- [Anthropic: Writing Tools for AI Agents](https://www.anthropic.com/engineering/writing-tools-for-agents)

### OpenClaw
- [OpenClaw Skills文档](https://docs.openclaw.ai/skills)
- [ClawHub技能市场](https://clawhub.com)

---

*本讲义最后更新：2026-03-01*
# L11-12 补充章节：智能体通信协议详解

> **课程**: MBA大模型智能体课程  
> **补充来源**: Datawhale Hello-Agents 第十章  
> **适用**: 讲师备课 / 学生深入学习

---

## 背景

原有L11-12讲义主要介绍了MCP协议和Skill系统，但缺少：
1. **A2A协议详解**（Google提出的Agent-to-Agent协议）
2. **ANP协议介绍**（大规模智能体网络）
3. **三种协议对比表格**（MCP/A2A/ANP）
4. **MCP传输方式详解**（Stdio/HTTP/SSE）
5. **完整的MCP代码示例**

本补充章节将完善协议生态全景，帮助学员理解和选择合适的通信协议。

---

## 补充内容

### Part 1: 智能体通信协议全景

#### 1.1 为什么需要多种协议？

当我们构建复杂的AI系统时，会遇到三类不同的通信需求：

```
┌─────────────────────────────────────────────────────────────┐
│                   智能体通信需求层次                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  层次1: 工具访问                                            │
│  ─────────────────                                          │
│  Agent 需要访问文件系统、数据库、API等外部资源                │
│  → MCP协议 (Model Context Protocol)                        │
│                                                             │
│  层次2: 点对点协作                                          │
│  ─────────────────                                          │
│  多个Agent之间需要对话、委托任务、协商能力                    │
│  → A2A协议 (Agent-to-Agent Protocol)                       │
│                                                             │
│  层次3: 网络化发现                                          │
│  ─────────────────                                          │
│  大规模Agent网络中，需要服务发现、路由选择、负载均衡           │
│  → ANP协议 (Agent Network Protocol)                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 1.2 三种协议设计理念对比

| 维度 | MCP | A2A | ANP |
|------|-----|-----|-----|
| **设计目标** | 标准化工具访问 | Agent间协作 | 大规模网络发现 |
| **架构模式** | Client-Server | 点对点(P2P) | 去中心化网络 |
| **核心抽象** | Tools/Resources/Prompts | Tasks/Artifacts | Services/Nodes/Routes |
| **通信方式** | JSON-RPC | HTTP/WebSocket | P2P协议 |
| **生态成熟度** | 高（Anthropic官方） | 中（Google官方） | 低（社区探索） |
| **适用规模** | 单Agent + 多工具 | 少量Agent协作 | 大规模Agent网络 |
| **发布方** | Anthropic (2024.11) | Google (2024.12) | 社区 |

---

### Part 2: A2A 协议详解

#### 2.1 协议背景与动机

A2A（Agent-to-Agent Protocol）由Google团队提出，解决的是**智能体之间如何协作**的问题。

**传统多Agent协作的痛点**：

```
传统方式：中央协调器（星型拓扑）
─────────────────────────────────
         ┌─────────────┐
         │  协调器      │
         │  (单点故障)  │
         └──────┬──────┘
            ┌───┼───┐
            ↓   ↓   ↓
          Agent Agent Agent
          
问题：
1. 单点故障：协调器失效导致系统瘫痪
2. 性能瓶颈：所有通信经过中心节点
3. 扩展困难：增加Agent需修改中心逻辑
```

**A2A的解决方案**：

```
A2A方式：点对点（网状拓扑）
─────────────────────────────────
         Agent ←───→ Agent
           ↑  ╲   ╱  ↑
           │   ╲ ╱   │
           │    ╳    │
           │   ╱ ╲   │
           ↓  ╱   ╲  ↓
         Agent ←───→ Agent
         
优势：
1. 去中心化：无单点故障
2. 高并发：直接通信，无瓶颈
3. 易扩展：新Agent自动加入网络
```

#### 2.2 A2A核心概念

| 概念 | 描述 | 类比 |
|------|------|------|
| **Task（任务）** | 可执行的工作单元，有生命周期 | 工单/Issue |
| **Artifact（工件）** | 任务产出物，可被其他Agent消费 | 交付物/文档 |
| **Skill（技能）** | Agent声明的能力 | 个人简历上的技能 |
| **Message（消息）** | Agent间通信的基本单元 | 聊天消息 |

#### 2.3 Task生命周期

```
                    ┌──────────────────────┐
                    │    CREATED (创建)     │
                    └──────────┬───────────┘
                               │
                    ┌──────────▼───────────┐
                    │  NEGOTIATING (协商)   │◄─────┐
                    └──────────┬───────────┘      │
                               │                   │
              ┌────────────────┼────────────────┐ │
              ↓                ↓                ↓ │
     ┌────────────────┐ ┌────────────┐ ┌──────────────┐
     │ DELEGATED(代理) │ │ ACCEPTED   │ │ REJECTED     │
     └───────┬────────┘ │ (接受)     │ │ (拒绝)       │
             │          └─────┬──────┘ └──────────────┘
             │                │
             └────────────────┤
                              ↓
                    ┌──────────────────────┐
                    │   RUNNING (执行中)    │
                    └──────────┬───────────┘
                               │
              ┌────────────────┼────────────────┐
              ↓                                 ↓
     ┌────────────────┐                ┌───────────────┐
     │ COMPLETED(完成) │                │ FAILED(失败)  │
     └────────────────┘                └───────────────┘
```

#### 2.4 A2A服务器实现

```python
from typing import Dict, Callable, Any, Optional
from dataclasses import dataclass, field
from datetime import datetime
from enum import Enum
import uuid

class TaskStatus(Enum):
    CREATED = "created"
    NEGOTIATING = "negotiating"
    ACCEPTED = "accepted"
    REJECTED = "rejected"
    RUNNING = "running"
    COMPLETED = "completed"
    FAILED = "failed"

@dataclass
class Task:
    """A2A任务对象"""
    task_id: str
    description: str
    status: TaskStatus = TaskStatus.CREATED
    requester: str = ""
    assignee: str = ""
    artifacts: list = field(default_factory=list)
    created_at: datetime = field(default_factory=datetime.now)
    completed_at: Optional[datetime] = None

class A2AServer:
    """
    A2A服务器 - Agent间通信的服务端
    
    特点：
    - 技能注册与发现
    - 任务生命周期管理
    - 工件(Artifact)交换
    """
    
    def __init__(
        self, 
        name: str, 
        description: str = "",
        version: str = "1.0.0",
        capabilities: Dict[str, Any] = None
    ):
        self.name = name
        self.description = description
        self.version = version
        self.capabilities = capabilities or {}
        
        self.skills: Dict[str, Callable] = {}
        self.tasks: Dict[str, Task] = {}
    
    def skill(self, name: str):
        """技能装饰器"""
        def decorator(func: Callable) -> Callable:
            self.skills[name] = func
            print(f"✅ 技能 '{name}' 已注册到 {self.name}")
            return func
        return decorator
    
    def create_task(self, description: str, requester: str) -> Task:
        """创建新任务"""
        task = Task(
            task_id=str(uuid.uuid4()),
            description=description,
            requester=requester
        )
        self.tasks[task.task_id] = task
        return task
    
    def accept_task(self, task_id: str) -> bool:
        """接受任务"""
        if task_id not in self.tasks:
            return False
        
        task = self.tasks[task_id]
        task.status = TaskStatus.ACCEPTED
        task.assignee = self.name
        return True
    
    def execute_skill(self, skill_name: str, input_text: str) -> Dict[str, Any]:
        """执行技能"""
        if skill_name not in self.skills:
            return {"error": f"技能 '{skill_name}' 不存在"}
        
        try:
            result = self.skills[skill_name](input_text)
            return {"result": result, "status": "success"}
        except Exception as e:
            return {"error": str(e), "status": "failed"}
    
    def get_agent_card(self) -> Dict[str, Any]:
        """获取Agent名片（用于服务发现）"""
        return {
            "name": self.name,
            "description": self.description,
            "version": self.version,
            "capabilities": self.capabilities,
            "skills": list(self.skills.keys())
        }
    
    def run(self, host: str = "localhost", port: int = 5000):
        """启动HTTP服务"""
        from flask import Flask, request, jsonify
        
        app = Flask(self.name)
        
        @app.route("/agent-card", methods=["GET"])
        def agent_card():
            return jsonify(self.get_agent_card())
        
        @app.route("/skill/<skill_name>", methods=["POST"])
        def call_skill(skill_name):
            data = request.json or {}
            result = self.execute_skill(skill_name, data.get("input", ""))
            return jsonify(result)
        
        @app.route("/tasks", methods=["POST"])
        def create_task_endpoint():
            data = request.json or {}
            task = self.create_task(
                description=data.get("description", ""),
                requester=data.get("requester", "unknown")
            )
            return jsonify({"task_id": task.task_id, "status": task.status.value})
        
        print(f"🚀 A2A服务器 '{self.name}' 启动在 http://{host}:{port}")
        app.run(host=host, port=port)
```

#### 2.5 A2A客户端实现

```python
import requests
from typing import Dict, Any

class A2AClient:
    """
    A2A客户端 - 用于与其他Agent通信
    """
    
    def __init__(self, agent_url: str):
        self.agent_url = agent_url.rstrip("/")
    
    def get_agent_card(self) -> Dict[str, Any]:
        """获取目标Agent的名片"""
        response = requests.get(f"{self.agent_url}/agent-card")
        return response.json()
    
    def execute_skill(self, skill_name: str, input_text: str) -> Dict[str, Any]:
        """调用目标Agent的技能"""
        response = requests.post(
            f"{self.agent_url}/skill/{skill_name}",
            json={"input": input_text}
        )
        return response.json()
    
    def create_task(self, description: str, requester: str) -> Dict[str, Any]:
        """在目标Agent上创建任务"""
        response = requests.post(
            f"{self.agent_url}/tasks",
            json={"description": description, "requester": requester}
        )
        return response.json()
```

#### 2.6 A2A完整示例

```python
# 示例：研究员和撰写员协作

import threading
import time

# 创建研究员Agent
researcher = A2AServer(
    name="研究员",
    description="负责信息搜索和分析",
    capabilities={"search": True, "analyze": True}
)

@researcher.skill("research")
def do_research(query: str) -> str:
    """执行研究任务"""
    return f"关于'{query}'的研究结果：找到了5篇相关论文和10个案例..."

# 创建撰写员Agent
writer = A2AServer(
    name="撰写员",
    description="负责内容创作",
    capabilities={"writing": True, "editing": True}
)

@writer.skill("write")
def do_write(content: str) -> str:
    """执行写作任务"""
    return f"基于研究内容，撰写了一篇1500字的报告..."

# 启动服务
threading.Thread(target=lambda: researcher.run(port=5001), daemon=True).start()
threading.Thread(target=lambda: writer.run(port=5002), daemon=True).start()
time.sleep(2)

# 协作流程
researcher_client = A2AClient("http://localhost:5001")
writer_client = A2AClient("http://localhost:5002")

# 1. 获取Agent能力
print("研究员能力:", researcher_client.get_agent_card())
print("撰写员能力:", writer_client.get_agent_card())

# 2. 执行协作
research_result = researcher_client.execute_skill("research", "AI Agent发展趋势")
print("研究结果:", research_result)

write_result = writer_client.execute_skill("write", research_result["result"])
print("写作结果:", write_result)
```

---

### Part 3: ANP 协议介绍

#### 3.1 协议目标

ANP（Agent Network Protocol）专注于解决**大规模智能体网络**中的三个核心问题：

| 问题 | 描述 | ANP解决方案 |
|------|------|------------|
| **服务发现** | 如何找到能处理特定任务的Agent？ | 服务注册与发现机制 |
| **智能路由** | 多个Agent都能处理时，选择哪个？ | 基于负载/成本的路由算法 |
| **动态扩展** | 新Agent如何加入网络？ | 去中心化的节点管理 |

#### 3.2 ANP核心概念

```
┌─────────────────────────────────────────────────────────────┐
│                    ANP 架构示意图                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   ┌──────────┐     ┌──────────┐     ┌──────────┐           │
│   │ Agent A  │     │ Agent B  │     │ Agent C  │           │
│   │ NLP服务  │     │ 图像服务  │     │ 数据服务  │           │
│   └────┬─────┘     └────┬─────┘     └────┬─────┘           │
│        │                │                │                  │
│        └────────────────┼────────────────┘                  │
│                         │                                   │
│                  ┌──────▼──────┐                            │
│                  │ Discovery   │  ← 服务发现中心             │
│                  │  Service    │                            │
│                  └──────┬──────┘                            │
│                         │                                   │
│        ┌────────────────┼────────────────┐                  │
│        │                │                │                  │
│   ┌────▼────┐     ┌─────▼────┐    ┌─────▼────┐             │
│   │ Client1 │     │ Client2  │    │ Client3  │             │
│   └─────────┘     └──────────┘    └──────────┘             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

| 概念 | 描述 |
|------|------|
| **ServiceNode** | 提供服务的Agent节点 |
| **Discovery** | 服务发现中心（可分布式部署） |
| **Route** | 服务路由策略 |
| **ServiceCard** | Agent能力声明卡片 |

#### 3.3 ANP服务发现实现

```python
from typing import Dict, List, Optional
from dataclasses import dataclass, field
from datetime import datetime
import threading

@dataclass
class ServiceInfo:
    """服务信息"""
    service_id: str
    service_name: str
    service_type: str
    capabilities: List[str]
    endpoint: str
    metadata: Dict = field(default_factory=dict)
    registered_at: datetime = field(default_factory=datetime.now)
    last_heartbeat: datetime = field(default_factory=datetime.now)

class ANPDiscovery:
    """
    ANP服务发现中心
    
    功能：
    - 服务注册与注销
    - 服务发现与查询
    - 健康检查
    """
    
    def __init__(self):
        self.services: Dict[str, ServiceInfo] = {}
        self._lock = threading.Lock()
    
    def register_service(
        self,
        service_id: str,
        service_name: str,
        service_type: str,
        capabilities: List[str],
        endpoint: str,
        metadata: Dict = None
    ) -> bool:
        """注册服务"""
        with self._lock:
            self.services[service_id] = ServiceInfo(
                service_id=service_id,
                service_name=service_name,
                service_type=service_type,
                capabilities=capabilities,
                endpoint=endpoint,
                metadata=metadata or {}
            )
            print(f"✅ 服务 '{service_name}' 已注册 (ID: {service_id})")
            return True
    
    def unregister_service(self, service_id: str) -> bool:
        """注销服务"""
        with self._lock:
            if service_id in self.services:
                del self.services[service_id]
                return True
            return False
    
    def discover_services(
        self,
        service_type: str = None,
        capability: str = None
    ) -> List[ServiceInfo]:
        """发现服务"""
        results = []
        
        for service in self.services.values():
            # 按类型过滤
            if service_type and service.service_type != service_type:
                continue
            
            # 按能力过滤
            if capability and capability not in service.capabilities:
                continue
            
            results.append(service)
        
        return results
    
    def get_best_service(
        self,
        service_type: str,
        strategy: str = "load"
    ) -> Optional[ServiceInfo]:
        """
        获取最佳服务（支持负载均衡）
        
        策略：
        - load: 选择负载最低的
        - cost: 选择成本最低的
        - random: 随机选择
        """
        services = self.discover_services(service_type=service_type)
        
        if not services:
            return None
        
        if strategy == "load":
            return min(services, key=lambda s: s.metadata.get("load", 1.0))
        elif strategy == "cost":
            return min(services, key=lambda s: s.metadata.get("cost", 1.0))
        else:
            import random
            return random.choice(services)
    
    def heartbeat(self, service_id: str) -> bool:
        """心跳更新"""
        with self._lock:
            if service_id in self.services:
                self.services[service_id].last_heartbeat = datetime.now()
                return True
            return False
    
    def list_all_services(self) -> List[ServiceInfo]:
        """列出所有服务"""
        return list(self.services.values())
```

#### 3.4 ANP网络管理

```python
from typing import Set, Tuple

class ANPNetwork:
    """
    ANP网络管理器
    
    功能：
    - 节点管理
    - 连接管理
    - 网络拓扑
    """
    
    def __init__(self, network_id: str):
        self.network_id = network_id
        self.nodes: Dict[str, str] = {}  # node_id -> endpoint
        self.connections: Set[Tuple[str, str]] = set()  # (from, to)
    
    def add_node(self, node_id: str, endpoint: str):
        """添加节点"""
        self.nodes[node_id] = endpoint
        print(f"✅ 节点 '{node_id}' 已加入网络")
    
    def remove_node(self, node_id: str):
        """移除节点"""
        if node_id in self.nodes:
            del self.nodes[node_id]
            # 同时移除相关连接
            self.connections = {
                (a, b) for a, b in self.connections 
                if a != node_id and b != node_id
            }
    
    def connect_nodes(self, from_node: str, to_node: str):
        """建立节点连接"""
        if from_node in self.nodes and to_node in self.nodes:
            self.connections.add((from_node, to_node))
    
    def get_neighbors(self, node_id: str) -> List[str]:
        """获取邻居节点"""
        return [
            to_node for from_node, to_node in self.connections
            if from_node == node_id
        ] + [
            from_node for from_node, to_node in self.connections
            if to_node == node_id
        ]
    
    def get_network_stats(self) -> Dict:
        """获取网络统计"""
        return {
            "network_id": self.network_id,
            "total_nodes": len(self.nodes),
            "total_connections": len(self.connections),
            "nodes": list(self.nodes.keys())
        }
```

#### 3.5 ANP完整示例

```python
# 示例：构建分布式计算网络

import random

# 1. 创建服务发现中心
discovery = ANPDiscovery()

# 2. 注册计算节点
for i in range(10):
    discovery.register_service(
        service_id=f"compute_node_{i}",
        service_name=f"计算节点{i}",
        service_type="compute",
        capabilities=["data_processing", "ml_training"],
        endpoint=f"http://node{i}:8000",
        metadata={
            "load": random.uniform(0.1, 0.9),
            "cpu_cores": random.choice([4, 8, 16]),
            "memory_gb": random.choice([16, 32, 64]),
            "gpu": random.choice([True, False]),
            "cost": random.uniform(0.01, 0.1)
        }
    )

print(f"\n✅ 注册了 {len(discovery.list_all_services())} 个计算节点")

# 3. 创建网络
network = ANPNetwork(network_id="ai_cluster")
for service in discovery.list_all_services():
    network.add_node(service.service_id, service.endpoint)

# 4. 智能任务分配
def assign_task(task_type: str):
    """根据任务类型选择最佳节点"""
    if task_type == "ml_training":
        # ML训练：选择有GPU且负载低的节点
        gpu_services = [
            s for s in discovery.discover_services(service_type="compute")
            if s.metadata.get("gpu") and s.metadata.get("load", 1) < 0.7
        ]
        if gpu_services:
            best = min(gpu_services, key=lambda s: s.metadata["load"])
            return best
    
    # 默认：选择负载最低的节点
    return discovery.get_best_service("compute", strategy="load")

# 5. 测试任务分配
print("\n--- 任务分配测试 ---")
for task in ["ml_training", "data_processing", "ml_training"]:
    best_node = assign_task(task)
    if best_node:
        print(f"任务 '{task}' → {best_node.service_name}")
        print(f"  负载: {best_node.metadata['load']:.2f}")
        print(f"  GPU: {'是' if best_node.metadata['gpu'] else '否'}")
        # 模拟负载增加
        best_node.metadata["load"] += 0.1
```

---

### Part 4: MCP传输方式详解

#### 4.1 传输方式对比

MCP协议的一个重要特性是**传输层无关性**，支持多种传输方式：

| 传输方式 | 适用场景 | 特点 | 性能 |
|---------|---------|------|------|
| **Stdio** | 本地进程通信 | 简单安全、无需网络 | 高 |
| **HTTP** | 远程服务调用 | 标准REST、需认证 | 中 |
| **SSE** | 实时流式传输 | 服务器推送、长连接 | 中 |
| **WebSocket** | 双向实时通信 | 全双工、低延迟 | 高 |
| **Memory** | 进程内通信 | 测试用、无序列化 | 极高 |

#### 4.2 Stdio传输（最常用）

```python
# Stdio传输示例 - 连接本地MCP服务器

from hello_agents.tools import MCPTool

# 方式1：连接自定义Python服务器
mcp_tool = MCPTool(
    name="custom",
    server_command=["python", "my_mcp_server.py"]
)

# 方式2：连接社区服务器（文件系统）
fs_tool = MCPTool(
    name="fs",
    server_command=[
        "npx", "-y", 
        "@modelcontextprotocol/server-filesystem", 
        "."  # 工作目录
    ]
)

# 方式3：连接GitHub服务器
github_tool = MCPTool(
    name="gh",
    server_command=[
        "npx", "-y",
        "@modelcontextprotocol/server-github"
    ]
    # 需要设置环境变量 GITHUB_PERSONAL_ACCESS_TOKEN
)
```

#### 4.3 HTTP传输

```python
# HTTP传输示例 - 连接远程MCP服务器

import asyncio
from hello_agents.protocols import MCPClient

async def use_http_transport():
    # 连接到远程HTTP MCP服务器
    client = MCPClient("http://api.example.com/mcp")
    
    async with client:
        # 获取可用工具
        tools = await client.list_tools()
        print(f"远程服务器提供 {len(tools)} 个工具")
        
        # 调用远程工具
        result = await client.call_tool(
            "process_data",
            {"input": "Hello", "operation": "uppercase"}
        )
        print(f"结果: {result}")

# asyncio.run(use_http_transport())
```

#### 4.4 SSE传输（流式）

```python
# SSE传输示例 - 支持流式响应

async def use_sse_transport():
    """SSE适合需要实时反馈的场景"""
    
    client = MCPClient(
        "http://localhost:8080/sse",
        transport_type="sse"
    )
    
    async with client:
        # SSE支持流式处理
        result = await client.call_tool(
            "stream_process",
            {"input": "大量数据", "stream": True}
        )
        print(f"流式处理完成: {result}")
```

---

### Part 5: MCP完整代码示例

#### 5.1 创建自定义MCP服务器

```python
# my_calculator_server.py - 自定义MCP服务器

from mcp.server import Server
from mcp.server.stdio import stdio_server
from mcp.types import Tool, TextContent
import json

# 创建服务器实例
server = Server("calculator")

# 定义工具
@server.list_tools()
async def list_tools():
    """列出所有可用工具"""
    return [
        Tool(
            name="add",
            description="加法计算器",
            inputSchema={
                "type": "object",
                "properties": {
                    "a": {"type": "number", "description": "第一个数"},
                    "b": {"type": "number", "description": "第二个数"}
                },
                "required": ["a", "b"]
            }
        ),
        Tool(
            name="multiply",
            description="乘法计算器",
            inputSchema={
                "type": "object",
                "properties": {
                    "a": {"type": "number"},
                    "b": {"type": "number"}
                },
                "required": ["a", "b"]
            }
        ),
        Tool(
            name="get_system_info",
            description="获取系统信息",
            inputSchema={"type": "object", "properties": {}}
        )
    ]

@server.call_tool()
async def call_tool(name: str, arguments: dict):
    """执行工具"""
    
    if name == "add":
        a = float(arguments.get("a", 0))
        b = float(arguments.get("b", 0))
        result = a + b
        return [TextContent(type="text", text=str(result))]
    
    elif name == "multiply":
        a = float(arguments.get("a", 0))
        b = float(arguments.get("b", 0))
        result = a * b
        return [TextContent(type="text", text=str(result))]
    
    elif name == "get_system_info":
        import platform
        info = {
            "system": platform.system(),
            "python": platform.python_version(),
            "machine": platform.machine()
        }
        return [TextContent(type="text", text=json.dumps(info, ensure_ascii=False))]
    
    else:
        return [TextContent(type="text", text=f"未知工具: {name}")]

# 启动服务器
async def main():
    async with stdio_server() as (read_stream, write_stream):
        await server.run(read_stream, write_stream)

if __name__ == "__main__":
    import asyncio
    asyncio.run(main())
```

#### 5.2 在Agent中使用MCP工具

```python
# 示例：智能计算助手

from hello_agents import SimpleAgent, HelloAgentsLLM
from hello_agents.tools import MCPTool
from dotenv import load_dotenv

load_dotenv()

# 创建Agent
agent = SimpleAgent(
    name="计算助手",
    llm=HelloAgentsLLM(),
    system_prompt="你是一个智能计算助手，可以进行数学计算。"
)

# 添加自定义MCP工具
calculator_tool = MCPTool(
    name="calc",
    description="数学计算工具",
    server_command=["python", "my_calculator_server.py"]
)
agent.add_tool(calculator_tool)

# 使用
response = agent.run("请计算 25 乘以 16，然后加上 100")
print(response)
```

#### 5.3 多Agent协作示例

```python
# 示例：GitHub搜索 + 报告生成

from hello_agents import SimpleAgent, HelloAgentsLLM
from hello_agents.tools import MCPTool
import threading
import time

# Agent 1: GitHub搜索专家
github_searcher = SimpleAgent(
    name="GitHub搜索专家",
    llm=HelloAgentsLLM(),
    system_prompt="你是GitHub搜索专家，负责搜索相关仓库。"
)

github_tool = MCPTool(
    name="gh",
    server_command=["npx", "-y", "@modelcontextprotocol/server-github"]
)
github_searcher.add_tool(github_tool)

# Agent 2: 报告生成专家
report_writer = SimpleAgent(
    name="报告生成专家",
    llm=HelloAgentsLLM(),
    system_prompt="你是报告撰写专家，负责将搜索结果整理成报告。"
)

fs_tool = MCPTool(
    name="fs",
    server_command=["npx", "-y", "@modelcontextprotocol/server-filesystem", "."]
)
report_writer.add_tool(fs_tool)

# 协作流程
def create_research_report(topic: str):
    """创建研究报告"""
    
    # Step 1: 搜索
    print("🔍 搜索GitHub仓库...")
    search_result = github_searcher.run(
        f"搜索关于'{topic}'的GitHub仓库，返回前5个最相关的结果"
    )
    print(f"搜索结果:\n{search_result}\n")
    
    # Step 2: 生成报告
    print("📝 生成报告...")
    report = report_writer.run(f"""
    根据以下搜索结果，生成一份Markdown研究报告：
    
    {search_result}
    
    报告要求：
    1. 标题：{topic}研究报告
    2. 简介
    3. 主要发现（列出项目和特点）
    4. 总结
    """)
    print(f"报告内容:\n{report}")
    
    return report

# 执行
if __name__ == "__main__":
    create_research_report("AI Agent框架")
```

---

### Part 6: 协议选型建议

#### 6.1 决策树

```
你的需求是什么？
    │
    ├── 访问外部工具/API/数据库
    │   └── → 使用 MCP
    │       ├── 本地工具 → Stdio传输
    │       └── 远程服务 → HTTP传输
    │
    ├── 多个Agent协作完成任务
    │   └── → 使用 A2A
    │       ├── 简单协作 → 直接HTTP调用
    │       └── 复杂协作 → 完整Task管理
    │
    └── 大规模Agent网络管理
        └── → 使用 ANP
            ├── 服务发现 → ANPDiscovery
            └── 网络拓扑 → ANPNetwork
```

#### 6.2 实际场景推荐

| 场景 | 推荐方案 |
|------|---------|
| 智能客服系统 | MCP（访问知识库）+ A2A（多技能Agent协作） |
| 自动化运维 | MCP（访问服务器、数据库）|
| 研究助手 | MCP（搜索、文件操作）+ A2A（研究员+撰写员） |
| 大规模推理集群 | ANP（服务发现）+ A2A（任务分发） |
| 简单工具调用 | 仅MCP |

---

## 与原有内容的衔接

本补充章节应安排在原L11-12讲义的以下位置：

1. **Part 2（MCP协议原理）之后**：插入"Part 2.5: MCP传输方式详解"
2. **Part 3（Skill系统）之后**：插入"Part 3.5: A2A协议详解"
3. **Part 4（工具设计原则）之后**：插入"Part 4.5: ANP协议与网络管理"
4. **最后**：添加"Part 7: 三种协议对比与选型"

建议的课堂安排：
- MCP深入（含传输方式）：25分钟
- A2A协议讲解与Demo：25分钟
- ANP协议介绍：15分钟
- 协议选型讨论：15分钟
- 动手实践（选择一个协议实现简单Demo）：20分钟

---

*本补充章节最后更新：2026-03-06*
